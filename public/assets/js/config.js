// assets/js/config.js

// Static site key for Dominguez Tech Solutions
export const SITE_KEY = "domtech";

// Construct base URL from current origin (supports http and https)
export const API_BASE = `${window.location.protocol}//${window.location.host}`;